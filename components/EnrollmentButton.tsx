import React from "react";
import { View } from "react-native";
import { EnrollmentButtonProps } from "../@types/types";

const EnrollmentButton: React.FC<EnrollmentButtonProps> = ({
  activityGuid,
}) => {
  return <View></View>;
};
